import React from "react";

import "./product-list-item.css";

const ProductListItem = ({product}) => {
    const {name, manufacturer, price, coverImage} = product
    return (
        <div>
            <p>Hello</p>
        </div>
    );
}

export default ProductListItem;
    